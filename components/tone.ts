import type { Tone } from "@/content/types";

export const toneBgClass: Record<Tone, string> = {
  default: "",
  soft: "bg-[color:var(--color-soft)] text-black",
  panel: "bg-[color:var(--color-panel)]",
};
